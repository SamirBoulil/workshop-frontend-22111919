import React from "react";

// const ProductList = ({products}: {products: Product}) => (
//     {products.map(product =>
//           React.cloneElement(React.Children.only(Poster), {
//             ...product,
//             key: product.id
//           })
//         )}

// )
